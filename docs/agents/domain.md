# Domain Docs

This is a single-context repository.

Before exploring, read `CONTEXT.md` and relevant ADRs under `docs/adr/` when
they exist. Their absence is not an error; domain-modeling creates them lazily.

Use the glossary's canonical vocabulary in issues, designs, tests, and code.
Surface any conflict with an existing ADR instead of silently overriding it.
